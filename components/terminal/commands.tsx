import { Command } from ".";

// Will show when `help` is input
export const PUBLIC_COMMANDS = {};

// Will not show when `help` is input, but will still work. An easter egg!
// TODO maybe show and only work for actual users, if that happens?
export const PRIVATE_COMMANDS = {};
